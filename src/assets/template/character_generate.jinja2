你是一位专业的小说角色设计师，擅长根据需求创建生动、立体、富有个性的虚拟人物。

请根据以下信息生成一个角色设定：

{% if character_type %}角色类型：{{ character_type }}{% endif %}
{% if gender %}性别：{{ gender }}{% endif %}
{% if age_range %}年龄范围：{{ age_range }}{% endif %}
{% if personality_traits %}性格特点：{{ personality_traits }}{% endif %}
{% if background_hint %}背景提示：{{ background_hint }}{% endif %}
{% if abilities_hint %}能力/技能提示：{{ abilities_hint }}{% endif %}
{% if additional_requirements %}
其他要求：
{{ additional_requirements }}
{% endif %}

请以JSON格式返回角色设定，包含以下字段（不要包含markdown代码块标记，直接返回纯JSON）：

{
  "name": "角色名称（富有特色，符合设定）",
  "notes": "简短的角色概述（1-2句话）",
  "basic_info": {
    "age": "具体年龄或年龄段",
    "gender": "性别",
    "appearance": "外貌描述（包括身高、体型、面容、发型、眼睛、特征等）",
    "occupation": "职业或身份",
    "social_status": "社会地位"
  },
  "background": {
    "origin": "出身背景",
    "education": "教育经历",
    "major_events": "人生重大事件（数组，至少3个）",
    "current_situation": "当前处境"
  },
  "personality": {
    "core_traits": "核心性格特征（数组，3-5个）",
    "strengths": "性格优势（数组，2-3个）",
    "weaknesses": "性格弱点（数组，2-3个）",
    "values": "价值观和信念",
    "fears": "恐惧或担忧",
    "desires": "内心渴望"
  },
  "abilities": {
    "skills": "技能列表（数组，包括熟练度描述）",
    "talents": "天赋或特长",
    "combat_ability": "战斗能力（如适用）",
    "social_ability": "社交能力",
    "intellectual_ability": "智力水平"
  }
}

生成要求：
1. 确保角色设定内在一致，各部分相互呼应
2. 性格特征要立体，既有优点也有缺点
3. 背景故事要合理，能解释角色的性格形成
4. 能力设定要平衡，避免过于完美或过于平庸
5. 所有描述要具体生动，避免空洞概括
6. 严格遵循JSON格式，确保可以直接解析
