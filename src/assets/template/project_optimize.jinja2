{%- if optimization_type == 'grammar' %}
请检查并修正以下内容的语法错误、错别字和标点符号问题：

{{ content }}

请直接返回修正后的内容，不要添加任何解释。
{%- elif optimization_type == 'style' %}
请优化以下内容的写作风格，使其更加生动、流畅、有文采：

{{ content }}

请直接返回优化后的内容，不要添加任何解释。
{%- else %}
请全面优化以下内容，包括语法、风格、逻辑性和可读性：

{{ content }}

请直接返回优化后的内容，不要添加任何解释。
{%- endif %}
